#<!DOCTYPE html>
<html lang="ur" dir="ltr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Startup Solutions 2026 | We Solve Your Business Problems</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body class="bg-gray-50 font-sans text-gray-900">

    <nav class="bg-white shadow-md p-4 sticky top-0 z-50">
        <div class="container mx-auto flex justify-between items-center">
            <h1 class="text-2xl font-bold text-blue-600 tracking-tight">SOLO FOUNDER 2026</h1>
            <div class="space-x-6">
                <a href="index.html" class="hover:text-blue-600 font-medium">Home</a>
                <a href="#about" class="hover:text-blue-600 font-medium">About</a>
                <a href="#reviews" class="hover:text-blue-600 font-medium">Reviews</a>
                <a href="#contact" class="hover:text-blue-600 font-medium">Contact</a>
            </div>
        </div>
    </nav>

    <header class="bg-white py-20 border-b">
        <div class="container mx-auto text-center px-4">
            <h2 class="text-4xl md:text-6xl font-extrabold text-gray-800 mb-6">
                Kya aapke business ko koi <span class="text-blue-600">Problem</span> hai?
            </h2>
            <p class="text-xl text-gray-600 mb-10 max-w-2xl mx-auto">
                Hum yahan naye startups ki madad karne aur unke mushkilat ko hal karne ke liye hain. Transparency aur trust ke sath apne business ko scale karein.
            </p>
            <a href="#problem-form" class="bg-blue-600 text-white px-8 py-4 rounded-full text-lg font-semibold hover:bg-blue-700 transition duration-300 shadow-lg">
                Submit Your Problem Now
            </a>
        </div>
    </header>

    <section class="py-16 bg-gray-50">
        <div class="container mx-auto px-4">
            <h3 class="text-3xl font-bold text-center mb-12">Humein Kyun Chunein? (Benefits)</h3>
            <div class="grid md:grid-cols-3 gap-8 text-center">
                
                <div class="bg-white p-8 rounded-xl shadow-sm hover:shadow-md transition">
                    <div class="text-blue-600 text-4xl mb-4"><i class="fas fa-shield-alt"></i></div>
                    <h4 class="text-xl font-bold mb-3">Mukammal Shaffafiyat</h4>
                    <p class="text-gray-600">Aaj kal ke fraud ke daur mein, hum trust aur honesty ke sath kaam karte hain.</p>
                </div>

                <div class="bg-white p-8 rounded-xl shadow-sm hover:shadow-md transition">
                    <div class="text-blue-600 text-4xl mb-4"><i class="fas fa-rocket"></i></div>
                    <h4 class="text-xl font-bold mb-3">Scaling Expert</h4>
                    <p class="text-gray-600">Hum aapke startup ko agle level par le jane mein aapki madad karte hain.</p>
                </div>

                <div class="bg-white p-8 rounded-xl shadow-sm hover:shadow-md transition">
                    <div class="text-blue-600 text-4xl mb-4"><i class="fas fa-comments"></i></div>
                    <h4 class="text-xl font-bold mb-3">Direct Raabta</h4>
                    <p class="text-gray-600">Bina kisi rukawat ke seedha mujh se baat karein aur apna masla hal karwayein.</p>
                </div>

            </div>
        </div>
    </section>

    <footer class="bg-gray-800 text-white py-8 text-center">
        <p>&copy; 2026 Solo Founder Startup. All Rights Reserved.</p>
    </footer>

</body>
</html> Simple workflow for deploying static content to GitHub Pages
name: Deploy static content to Pages

on:
  # Runs on pushes targeting the default branch
  push:
    branches: ["main"]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # Single deploy job since we're just deploying
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # Upload entire repository
          path: '.'
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
